<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Products</title>
</head>

<body>
    <h1>
        <%= category%> Products!
    </h1>
    <ul>
        <% for(let product of products) { %>
            <li><a href="buyersInterface/products/<%=product._id%>">
                    <%= product.name %>
                </a> </li>
            <% }%>
    </ul>
    <%if(category !=='All' ) {%>
        <a href="/buyersInterface">All Products</a>
        <% } %>
  <button type="" name="button"> <a href="/logout">logout</a> </button>
</body>

</html>
